# Chat View - Nuxt 3 Frontend

채팅 플랫폼의 프론트엔드 애플리케이션입니다.

## 📋 프로젝트 개요

### 백엔드 서버 구성
- **chat-system-server** (8082): 채널 관리, 메시지 조회 (Query)
- **chat-message-server** (8081): 메시지 발송 (Command)
- **chat-websocket-server** (8083): 실시간 WebSocket 통신

### 주요 기능
1. **인증/인가**: JWT 기반 인증
2. **채널 관리**: 채널 생성, 조회, 관리
3. **실시간 채팅**: WebSocket 기반 양방향 통신
4. **메시지 기능**: 메시지 전송, 수신, 조회

## 🚀 시작하기

### 설치
```bash
npm install
```

### 개발 서버 실행
```bash
npm run dev
```

개발 서버: http://localhost:3000

### 빌드
```bash
npm run build
```

### 프로덕션 미리보기
```bash
npm run preview
```

## 📁 프로젝트 구조

```
chat-view/
├── assets/              # 정적 자산 (CSS, 이미지 등)
│   └── css/
│       └── main.css     # 전역 스타일
├── components/          # Vue 컴포넌트
│   ├── chat/           # 채팅 관련 컴포넌트
│   ├── channel/        # 채널 관련 컴포넌트
│   └── common/         # 공통 컴포넌트
├── composables/         # Composition API 재사용 로직
│   ├── useAuth.ts      # 인증 관련
│   ├── useChat.ts      # 채팅 관련
│   └── useWebSocket.ts # WebSocket 관련
├── layouts/             # 레이아웃 컴포넌트
│   ├── default.vue     # 기본 레이아웃
│   └── auth.vue        # 인증 레이아웃
├── middleware/          # 라우트 미들웨어
│   └── auth.ts         # 인증 미들웨어
├── pages/               # 페이지 컴포넌트 (자동 라우팅)
│   ├── index.vue       # 홈/로그인
│   ├── chat/
│   │   └── [id].vue    # 채팅방
│   └── channels/
│       └── index.vue   # 채널 목록
├── plugins/             # Nuxt 플러그인
│   └── api.ts          # API 클라이언트
├── stores/              # Pinia 상태 관리
│   ├── auth.ts         # 인증 스토어
│   ├── channel.ts      # 채널 스토어
│   ├── message.ts      # 메시지 스토어
│   └── websocket.ts    # WebSocket 스토어
├── types/               # TypeScript 타입 정의
│   ├── api.ts          # API 타입
│   ├── channel.ts      # 채널 타입
│   └── message.ts      # 메시지 타입
└── utils/               # 유틸리티 함수
    ├── api.ts          # API 호출 헬퍼
    └── websocket.ts    # WebSocket 헬퍼
```

## 🔧 주요 기술 스택

- **Nuxt 3**: Vue 3 기반 프레임워크
- **TypeScript**: 타입 안전성
- **Pinia**: 상태 관리
- **Tailwind CSS**: 유틸리티 기반 CSS
- **WebSocket**: 실시간 통신

## 📝 구현 섹션

### 1. 인증 시스템 (JWT)
- [x] JWT 토큰 관리 (저장, 갱신, 삭제)
- [x] 로그인/로그아웃 UI 및 로직
- [x] 인증 미들웨어
- [x] 인증 상태 관리 (Pinia Store)

### 2. API 통신 레이어
- [x] API 클라이언트 설정
- [x] 인터셉터 (토큰 자동 첨부)
- [x] 에러 핸들링
- [x] 타입 정의

### 3. WebSocket 연결 관리
- [x] WebSocket 연결/해제
- [x] 메시지 송수신
- [x] 재연결 로직
- [x] 연결 상태 관리

### 4. 채널 관리
- [x] 채널 목록 조회
- [x] 채널 생성 (Direct, Group, Public, Private)
- [x] 채널 상세 정보
- [x] 채널 UI 컴포넌트

### 5. 채팅 기능
- [x] 메시지 목록 (커서 페이징)
- [x] 메시지 전송
- [x] 실시간 메시지 수신
- [x] 채팅 UI 컴포넌트

### 6. UI/UX
- [x] 반응형 레이아웃
- [x] 채팅방 디자인
- [x] 메시지 버블
- [x] 입력창 및 전송 버튼
- [x] 로딩 상태 표시

## 🔌 API 엔드포인트

### System Server (8082)
- `GET /api/v1/channels`: 채널 목록
- `POST /api/v1/channels/direct`: 일대일 채널 생성
- `POST /api/v1/channels/group`: 그룹 채널 생성
- `GET /api/v1/messages?channelId={id}`: 메시지 조회

### Message Server (8081)
- `POST /api/messages`: 메시지 전송

### WebSocket Server (8083)
- `WS /ws/chat`: WebSocket 연결

## 🎨 디자인 가이드

### 컬러 팔레트
- Primary: Blue (#3B82F6)
- Secondary: Gray (#6B7280)
- Success: Green (#10B981)
- Warning: Yellow (#F59E0B)
- Error: Red (#EF4444)

### 레이아웃 구조
```
┌─────────────────────────────────┐
│         Header (로고, 유저)       │
├──────────┬──────────────────────┤
│          │                      │
│ Channel  │   Chat Messages      │
│  List    │                      │
│          │                      │
│          ├──────────────────────┤
│          │   Message Input      │
└──────────┴──────────────────────┘
```

## 📌 TODO

- [ ] 프로필 이미지 업로드
- [ ] 이모지 지원
- [ ] 파일 전송
- [ ] 알림 기능
- [ ] 검색 기능
- [ ] 다크 모드
